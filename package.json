{
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd SERVER && node app.js",
    "client": "cd CLIENT/react-app && npm run dev"
  }
}
